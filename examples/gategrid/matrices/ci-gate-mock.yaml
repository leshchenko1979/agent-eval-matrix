name: ci-gate-mock
profiles:
  - demo
models:
  - mock
case_sets:
  - demo
gate:
  baseline: demo
  regression:
    baseline: demo
    bounds:
      overall:
        pass_rate_min_delta: -0.05
      like_for_like:
        pass_rate_min_delta: -0.05
        max_regressed_cells: 1
  limits:
    overall:
      pass_rate_min: 0.5
